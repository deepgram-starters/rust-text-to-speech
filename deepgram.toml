[meta]
title = "Rust Text-to-Speech"
description = "Get started using Deepgram's Text-to-Speech with this Rust demo app"
author = "Deepgram DX Team <devrel@deepgram.com> (https://developers.deepgram.com)"
repository = "https://github.com/deepgram-starters/rust-text-to-speech"
useCase = "text-to-speech"
language = "rust"
framework = "axum"
sdk = "N/A"
tags = ["text-to-speech", "tts", "audio-generation", "rust", "axum"]

[install.pre]
command = "git submodule update --init --recursive"
message = "Submodules initialized"

[install]
command = ["cargo build --release", "cd frontend && corepack pnpm install"]
message = "Dependencies installed"

[install.config]
sample = "sample.env"
output = ".env"

[check]
command = ["command -v git", "command -v rustc", "command -v cargo", "command -v pnpm"]
message = "Prerequisites checked"

[start.pre]
command = ["test -f .env || echo 'Error: .env file not found'", "test -d frontend/.git || echo 'Error: Frontend submodule not initialized'"]
message = "Pre-flight checks complete"

[start]
command = ["cargo run", "cd frontend && corepack pnpm run dev -- --port 8080 --no-open"]
parallel = true
message = "Application running on http://localhost:8080"

[update]
command = "git submodule update --remote --merge"
message = "Submodules updated"

[clean]
command = ["rm -rf target", "rm -rf frontend/node_modules", "rm -rf frontend/dist"]
message = "Build artifacts cleaned"

[test]
command = "bash contracts/tests/run-text-to-speech-app.sh"
message = "Conformance tests passed"

# Eject frontend and contracts submodules for standalone development
[eject-frontend]
command = "make eject-frontend"
message = "Frontend ejected successfully"
